#Definitions of Abstract Cache structs
"""
    type: Abstract_Cache{M<:Model}

Top-level type for all caches.

Each concrete cache contains the `Model` and the fields to store the quantities
obtained from the model.
"""
abstract type Abstract_Cache{T, M<:Model} end #type was called AbstractCalculator

"""
    type: Abstract_ClassicalModel_Cache{T, M<:Union{ClassicalModel, CompositeModel}} <: Abstract_Cache{T, M}

Type for storing quantities obtained from models defined by an analytic classical potentials. Examples
using Base: AbstractArrayOrBroadcasted
include the harmonic potential (AdiabticModels.Harmonic), the morse potential (AdiabticModels.Morse) and
the Darling-Holloway elbow model (AdiabticModels.DarlingHollowayElbow)
"""
abstract type Abstract_ClassicalModel_Cache{T, M<:Union{ClassicalModel, ClassicalFrictionModel, CompositeModel}} <: Abstract_Cache{T, M} end #type was called AbstractAdiabaticCalculator

"""
    type: Abstract_QuantumModel_Cache{T, M} <: Abstract_Cache{T, M}

Abstract supertype for all quantum models that can be solved with exact diagonalisation.
"""
abstract type Abstract_QuantumModel_Cache{T, M<:Union{QuantumModel, QuantumFrictionModel}} <: Abstract_Cache{T, M} end #type was called AbstractDiabaticCalculator


function Base.show(io::Core.IO, cache::Abstract_Cache)
	printstyled(io, string(typeof(cache))*"\n"; bold=true)
	for field in fieldnames(typeof(cache))
		printstyled(io, "$(field):\n"; underline=true)
		Base.show(io, getfield(cache, field))
		print(io, "\n")
	end
end

#Definitions for the concrete Subtypes of the above Cache Types
"""
    struct: ClassicalModel_Cache{T,M} <: Abstract_ClassicalModel_Cache{T,M}
        model::{ClassicalModel, CompositeModel}
        potential::T,Matrix{T}}
        derivative::Matrix{T},Matrix{T}}


Struct of type Abstract_ClassicalModel_Cache, used to store quantities (of data type `T`) generated by its input model `M`.
"""
struct ClassicalModel_Cache{T,M} <: Abstract_ClassicalModel_Cache{T,M}
    model::M
    potential::Matrix{T}
    derivative::Matrix{T}

end

"""
    function: ClassicalModel_Cache{T}(model::M, atoms::Integer) where {T,M<:Model}

Function which constructs and returns the Struct of the same name. Takes as input an adiabatic model `M` of the users choice
and the number of atoms in the simulation
"""
function ClassicalModel_Cache(model::M, atoms::Integer, matrix_prototype::AbstractArray{T,N}) where {M<:Model, T, N}
    potential = similar(matrix_prototype, 1, 1)
    derivative = similar(matrix_prototype, ndofs(model), atoms)

    return ClassicalModel_Cache{T,M}(
        model,
        potential,
        derivative
    )
end


"""
    struct: RingPolymer_ClassicalModel_Cache{T,M} <: Abstract_ClassicalModel_Cache{T,M}
        model::M
        potential::Vector{Matrix{T}}
        derivative::Array{T,3}

        centroid::Matrix{T}
        centroid_potential::Hermitian{T,Matrix{T}}
        centroid_derivative::Matrix{Hermitian{T,Matrix{T}}}

Struct of type Abstract_ClassicalModel_Cache, used to store quantities (of data type `T`) generated by its input model `M`.
"""
struct RingPolymer_ClassicalModel_Cache{T,M} <: Abstract_ClassicalModel_Cache{T,M}
    model::M
    potential::Vector{Matrix{T}}
    derivative::Array{T,3}

    centroid::Matrix{T}
    centroid_potential::Matrix{T}
    centroid_derivative::Matrix{T}

end

"""
    function: RingPolymer_ClassicalModel_Cache{T}(model::M, atoms::Integer, beads::Integer) where {T,M<:Model}

Function which constructs and returns the Struct of the same name. Takes as input an adiabatic model `M` of the users choice,
the number of atoms in the simulation and the number of beads used in the ring polymer simulation.
"""
function RingPolymer_ClassicalModel_Cache(model::M, atoms::Integer, beads::Integer, matrix_prototype::AbstractArray{T,N}) where {M<:Model, T, N}
    potential = [similar(matrix_prototype, 1, 1) for i in 1:beads]
    derivative = similar(matrix_prototype, ndofs(model), atoms, beads)

    centroid = similar(matrix_prototype, ndofs(model), atoms)
    centroid_potential = similar(matrix_prototype, 1, 1)
    centroid_derivative = similar(matrix_prototype, ndofs(model), atoms)

    return RingPolymer_ClassicalModel_Cache{T,M}(
        model,
        potential,
        derivative,

        centroid,
        centroid_potential,
        centroid_derivative
    )
end

"""
    struct: ClassicalFrictionModel_Cache{T,M} <: Abstract_ClassicalModel_Cache{T,M}
        model::M
        potential::Matrix{T}
        derivative::Matrix{T},Matrix{T}}
        friction::Matrix{T},Matrix{T}}


Struct of type Abstract_ClassicalModel_Cache, used to store quantities (of data type `T`) generated by its input model `M`.
"""
struct ClassicalFrictionModel_Cache{T,M} <: Abstract_ClassicalModel_Cache{T,M} #was mutable struct - changed for efficiency, needs testing to determine if this is a code breaking change
    model::M
    potential::AbstractMatrix{T}
    derivative::AbstractMatrix{T}
    friction::AbstractMatrix{T}
end

"""
    function: ClassicalFrictionModel_Cache{T}(model::M, atoms::Integer) where {T,M<:Model}

Function which constructs and returns the struct of the same name. Takes as input an adiabatic friction model `M` of the users choice and
the number of atoms in the simulation.
"""
function ClassicalFrictionModel_Cache(model::M, atoms::Integer, matrix_prototype::AbstractMatrix) where {M<:Model}
    potential = similar(matrix_prototype, 1,1)
    derivative = similar(matrix_prototype, ndofs(model), atoms)
    friction = similar(matrix_prototype, ndofs(model)*atoms, ndofs(model)*atoms)

    return ClassicalFrictionModel_Cache(
        model,
        potential,
        derivative,
        friction
    )
end


"""
    struct: RingPolymer_ClassicalFrictionModel_Cache{T,M} <: Abstract_ClassicalModel_Cache{T,M}
        model::M
        potential::Vector{Matrix{T}}
        derivative::Matrix{T}
        friction::Matrix{T}

        centroid::Matrix{T}
        centroid_potential::Hermitian{T,Matrix{T}}
        centroid_derivative::Matrix{Hermitian{T,Matrix{T}}}
        centroid_adiabatic_derivative::Matrix{Matrix{T}}
        centroid_nonadiabatic_coupling::Matrix{Matrix{T}}


Struct of type Abstract_ClassicalModel_Cache, used to store quantities (of data type `T`) generated by its input model `M`.
"""
struct RingPolymer_ClassicalFrictionModel_Cache{T,M} <: Abstract_ClassicalModel_Cache{T,M}
    model::M
    potential::AbstractArray{T,3}
    derivative::AbstractArray{T,3}
    friction::AbstractArray{T,3}

    centroid::AbstractMatrix{T}
    centroid_potential::AbstractMatrix{T}
    centroid_derivative::AbstractMatrix{T}
    centroid_friction::AbstractMatrix{T}
end

"""
    function: RingPolymer_ClassicalFrictionModel_Cache{T}(model::M, atoms::Integer, beads::Integer) where {T,M<:Model}

Function which constructs and returns the struct of the same name. Takes as input an adiabatic model `M` of the users choice,
the number of atoms in the simulation and the number of beads used in the ring polymer simulation.
"""
function RingPolymer_ClassicalFrictionModel_Cache(model::M, atoms::Integer, beads::Integer, matrix_prototype::AbstractArray) where {M<:Model}

    potential = similar(matrix_prototype, 1, 1, beads)
    derivative = similar(matrix_prototype, ndofs(model), atoms, beads)
    friction = similar(matrix_prototype, ndofs(model)*atoms, ndofs(model)*atoms, beads)

    centroid = similar(matrix_prototype, ndofs(model), atoms)
    centroid_potential = similar(matrix_prototype, 1,1)
    centroid_derivative = similar(matrix_prototype, ndofs(model), atoms)
    centroid_friction = similar(matrix_prototype, ndofs(model)*atoms, ndofs(model)*atoms)

    return RingPolymer_ClassicalFrictionModel_Cache(
        model,
        potential,
        derivative,
        friction,

        centroid,
        centroid_potential,
        centroid_derivative,
        centroid_friction
    )
end

"""
    struct: QuantumModel_Cache{T,M} <: Abstract_QuantumModel_Cache{T,M}
        model::M
        potential::Hermitian{T,Matrix{T}}
        derivative::Matrix{Hermitian{T,Matrix{T}}}
        eigen::HermitianEigenWs{T, Matrix{T}, T}
        adiabatic_derivative::Matrix{Matrix{T}}
        nonadiabatic_coupling::Matrix{Matrix{T}}


Struct of type Abstract_QuantumModel_Cache, used to store quantities (of data types `T`, `S` and `L`) generated by its input model `M`.
"""
struct QuantumModel_Cache{T,M} <: Abstract_QuantumModel_Cache{T, M}
    model::M
    potential::AbstractMatrix
    derivative::AbstractMatrix
    eigen::HermitianEigenWs{T, Matrix{T}, T}
    phase_ref::AbstractVector{T}
    adiabatic_derivative::AbstractMatrix
    nonadiabatic_coupling::AbstractMatrix
    tmp_mat::AbstractMatrix

end

"""
    function: QuantumModel_Cache{T}(model::M, atoms::Integer) where {T,M<:Model}

Function which constructs and returns the Struct of the same name. Takes as input an adiabatic model `M` of the users choice
and the number of atoms in the simulation.
"""
function QuantumModel_Cache(model::M, atoms::Integer, matrix_prototype::AbstractArray{T,N}) where {M<:Model, T, N}
    n = nstates(model)
    mat = NQCModels.QuantumModels.matrix_template(model, T)
    vec = NQCModels.QuantumModels.vector_template(model, T)

    potential = Hermitian(zero(mat))
    derivative = [Hermitian(zero(mat)) for _=1:ndofs(model), _=1:atoms]
    eigen = HermitianEigenWs(zero(mat)+I, vecs=true)
    phase_ref = rand(T, n) .+ eps(T)
    adiabatic_derivative = [zero(mat) for _ in CartesianIndices(derivative)]
    nonadiabatic_coupling = [zero(mat) for _ in CartesianIndices(derivative)]
    tmp_mat = zero(mat)

    position = zeros(ndofs(model), atoms)

    return QuantumModel_Cache(
        model,
        potential,
        derivative,
        eigen,
        phase_ref,
        adiabatic_derivative,
        nonadiabatic_coupling,
        tmp_mat
    )
end


"""
    struct: RingPolymer_QuantumModel_Cache{T,M,S,L} <: Abstract_QuantumModel_Cache{T,M}
        model::M
        potential::Hermitian{T,Matrix{T}}
        derivative::Matrix{Hermitian{T,Matrix{T}}}
        eigen::HermitianEigenWs{T, Matrix{T}, T}
        adiabatic_derivative::Matrix{Matrix{T}}
        nonadiabatic_coupling::Matrix{Matrix{T}}

        traceless_potential::Vector{Hermitian{T,Matrix{T}}}
        V̄::Vector{T}
        traceless_derivative::Array{Hermitian{T,Matrix{T}},3}
        D̄::Array{T,3}
        traceless_adiabatic_derivative::Array{Matrix{T},3}

        centroid::Matrix{T}
        centroid_potential::Hermitian{T,Matrix{T}}
        centroid_derivative::Matrix{Hermitian{T,Matrix{T}}}
        centroid_eigen::HermitianEigenWs{T, Matrix{T}, T}
        centroid_adiabatic_derivative::Matrix{Matrix{T}}
        centroid_nonadiabatic_coupling::Matrix{Matrix{T}}



Struct of type Abstract_QuantumModel_Cache, used to store quantities (of data types `T`, `S` and `L`) generated by its input model `M`.
"""
struct RingPolymer_QuantumModel_Cache{T, M} <: Abstract_QuantumModel_Cache{T,M}
    model::M
    potential::Vector{Hermitian{T,Matrix{T}}}
    derivative::Array{Hermitian{T,Matrix{T}},3}
    eigen::Vector{HermitianEigenWs{T, Matrix{T}, T}}
    phase_ref::AbstractVector
    adiabatic_derivative::Array{Matrix{T},3}
    nonadiabatic_coupling::Array{Matrix{T},3}
    tmp_mat::Matrix{T}

    traceless_potential::Vector{Hermitian{T,Matrix{T}}}
    V̄::Vector{T}
    traceless_derivative::Array{Hermitian{T,Matrix{T}},3}
    D̄::Array{T,3}
    traceless_adiabatic_derivative::Array{Matrix{T},3}

    centroid::Matrix{T}
    centroid_potential::Hermitian{T,Matrix{T}}
    centroid_derivative::Matrix{Hermitian{T,Matrix{T}}}
    centroid_eigen::HermitianEigenWs{T, Matrix{T}, T}
    centroid_phase_ref::AbstractVector{T}
    centroid_adiabatic_derivative::Matrix{Matrix{T}}
    centroid_nonadiabatic_coupling::Matrix{Matrix{T}}
end

"""
    function: RingPolymer_QuantumModel_Cache{T}(model::M, atoms::Integer, beads::Integer) where {T,M<:Model}

Function which constructs and returns the Struct of the same name. Takes as input an adiabatic model `M` of the users choice,
the number of atoms in the simulation and the number of beads used in the ring polymer simulation.
"""
function RingPolymer_QuantumModel_Cache(model::M, atoms::Integer, beads::Integer, matrix_prototype::AbstractArray{T,N}) where {M<:Model, T, N}
    n = nstates(model)
    mat = NQCModels.QuantumModels.matrix_template(model, T)
    vec = NQCModels.QuantumModels.vector_template(model, T)

    potential = [Hermitian(zero(mat)) for _=1:beads]
    derivative = [Hermitian(zero(mat)) for _=1:ndofs(model), _=1:atoms, _=1:beads]
    adiabatic_derivative = [zero(mat) for _=1:ndofs(model), _=1:atoms, _=1:beads]
    eigen = [HermitianEigenWs(zero(mat)+I, vecs=true) for _=1:beads]
    phase_ref = [rand(T, n) .+ eps(T) for _=1:beads]
    nonadiabatic_coupling = [zero(mat) for _=1:ndofs(model), _=1:atoms, _=1:beads]
    tmp_mat = zero(mat)

    traceless_potential = [Hermitian(zero(mat)) for _=1:beads]
    V̄ = zeros(beads)
    traceless_derivative = [Hermitian(zero(mat)) for _=1:ndofs(model), _=1:atoms, _=1:beads]
    D̄ = zeros(ndofs(model), atoms, beads)
    traceless_adiabatic_derivative = [zero(mat) for _=1:ndofs(model), _=1:atoms, _=1:beads]

    centroid = zeros(T, ndofs(model), atoms)
    centroid_potential = Hermitian(zero(mat))
    centroid_derivative = [Hermitian(zero(mat)) for _=1:ndofs(model), _=1:atoms]
    centroid_eigen = HermitianEigenWs(zero(mat)+I, vecs=true)
    centroid_phase_ref = rand(T, n) .+ eps(T)
    centroid_adiabatic_derivative = [zero(mat) for _ in CartesianIndices(centroid_derivative)]
    centroid_nonadiabatic_coupling = [zero(mat) for _ in CartesianIndices(centroid_derivative)]

    position = zeros(ndofs(model), atoms, beads)

    return RingPolymer_QuantumModel_Cache(
        model,
        potential,
        derivative,
        eigen,
        phase_ref,
        adiabatic_derivative,
        nonadiabatic_coupling,
        tmp_mat,

        traceless_potential,
        V̄,
        traceless_derivative,
        D̄,
        traceless_adiabatic_derivative,

        centroid,
        centroid_potential,
        centroid_derivative,
        centroid_eigen,
        centroid_phase_ref,
        centroid_adiabatic_derivative,
        centroid_nonadiabatic_coupling
    )
end



"""
    struct: QuantumFrictionModel_Cache{T,M} <: Abstract_QuantumModel_Cache{T,M}
        model::M
        potential::Hermitian{T,Matrix{T}}
        derivative::Matrix{Hermitian{T,Matrix{T}}}
        eigen::HermitianEigenWs{T, Matrix{T}, T}
        adiabatic_derivative::Matrix{Matrix{T}}
        nonadiabatic_coupling::Matrix{Matrix{T}}


Struct of type Abstract_QuantumModel_Cache, used to store quantities (of data types `T`, `S` and `L`) generated by its input model `M`.
"""
struct QuantumFrictionModel_Cache{T,M} <: Abstract_QuantumModel_Cache{T,M}
    model::M
    friction_method::Union{FrictionEvaluationMethod, Nothing}
    potential::Hermitian{T,Matrix{T}}
    derivative::Matrix{Hermitian{T,Matrix{T}}}
    eigen::HermitianEigenWs{T, Matrix{T}, T}
    phase_ref::AbstractVector{T}
    adiabatic_derivative::Matrix{Matrix{T}}
    nonadiabatic_coupling::Matrix{Matrix{T}}
    friction::Matrix{T}
    tmp_mat::Matrix{T}
end

"""
    function: QuantumFrictionModel_Cache{T}(model::M, atoms::Integer) where {T,M<:Model}

Function which constructs and returns the Struct of the same name. Takes as input an adiabatic model `M` of the users choice
and the number of atoms in the simulation.
"""
function QuantumFrictionModel_Cache(model::M, atoms::Integer, matrix_prototype::AbstractArray; friction_method::Union{FrictionEvaluationMethod, Nothing}) where {M<:Model}
    n = nstates(model)
    mat = NQCModels.QuantumModels.matrix_template(model, eltype(matrix_prototype))
    vec = NQCModels.QuantumModels.vector_template(model, eltype(matrix_prototype))

    potential = Hermitian(zero(mat))
    derivative = [Hermitian(zero(mat)) for _=1:ndofs(model), _=1:atoms]
    eigen = HermitianEigenWs(zero(mat)+I, vecs=true)
    phase_ref = rand(eltype(matrix_prototype), n) .+ eps(eltype(matrix_prototype))
    adiabatic_derivative = [zero(mat) for _ in CartesianIndices(derivative)]
    nonadiabatic_coupling = [zero(mat) for _ in CartesianIndices(derivative)]
    friction = zeros(ndofs(model)*atoms, ndofs(model)*atoms)
    tmp_mat = zero(mat)

    if friction_method isa Nothing
        @warn "No friction evaluation algorithm has been specified, the friction tensor will be empty"
    end

    return QuantumFrictionModel_Cache(
        model,
        friction_method,
        potential,
        derivative,
        eigen,
        phase_ref,
        adiabatic_derivative,
        nonadiabatic_coupling,
        friction,
        tmp_mat
    )
end



"""
    struct: RingPolymer_QuantumFrictionModel_Cache{T,M,S,L} <: Abstract_QuantumModel_Cache{T,M}
        model::M
        potential::Hermitian{T,Matrix{T}}
        derivative::Matrix{Hermitian{T,Matrix{T}}}
        eigen::HermitianEigenWs{T, Matrix{T}, T}
        adiabatic_derivative::Matrix{Matrix{T}}
        nonadiabatic_coupling::Matrix{Matrix{T}}

        traceless_potential::Vector{Hermitian{T,Matrix{T}}}
        V̄::Vector{T}
        traceless_derivative::Array{Hermitian{T,Matrix{T}},3}
        D̄::Array{T,3}
        traceless_adiabatic_derivative::Array{Matrix{T},3}

        centroid::Matrix{T}
        centroid_potential::Hermitian{T,Matrix{T}}
        centroid_derivative::Matrix{Hermitian{T,Matrix{T}}}
        centroid_eigen::HermitianEigenWs{T, Matrix{T}, T}
        centroid_adiabatic_derivative::Matrix{Matrix{T}}
        centroid_nonadiabatic_coupling::Matrix{Matrix{T}}



Struct of type Abstract_QuantumModel_Cache, used to store quantities (of data types `T`, `S` and `L`) generated by its input model `M`.
"""
struct RingPolymer_QuantumFrictionModel_Cache{T,M} <: Abstract_QuantumModel_Cache{T,M}
    model::M
    friction_method::Union{FrictionEvaluationMethod, Nothing}
    potential::Vector{Hermitian{T,Matrix{T}}}
    derivative::Array{Hermitian{T,Matrix{T}},3}
    eigen::Vector{HermitianEigenWs{T, Matrix{T}, T}}
    phase_ref::Vector{AbstractVector{T}}
    adiabatic_derivative::Array{Matrix{T},3}
    nonadiabatic_coupling::Array{Matrix{T},3}
    friction::Array{Matrix{T},3}
    tmp_mat::Matrix{T}

    traceless_potential::Vector{Hermitian{T,Matrix{T}}}
    V̄::Vector{T}
    traceless_derivative::Array{Hermitian{T,Matrix{T}},3}
    D̄::Array{T,3}
    traceless_adiabatic_derivative::Array{Matrix{T},3}

    centroid::Matrix{T}
    centroid_potential::Hermitian{T,Matrix{T}}
    centroid_derivative::Matrix{Hermitian{T,Matrix{T}}}
    centroid_eigen::HermitianEigenWs{T, Matrix{T}, T}
    centroid_phase_ref::AbstractVector{T}
    centroid_adiabatic_derivative::Matrix{Matrix{T}}
    centroid_nonadiabatic_coupling::Matrix{Matrix{T}}
    centroid_friction::Matrix{T}
end

"""
    function: RingPolymer_QuantumModel_Cache{T}(model::M, atoms::Integer, beads::Integer) where {T,M<:Model}

Function which constructs and returns the Struct of the same name. Takes as input an adiabatic model `M` of the users choice,
the number of atoms in the simulation and the number of beads used in the ring polymer simulation.
"""
function RingPolymer_QuantumFrictionModel_Cache(model::M, atoms::Integer, beads::Integer, matrix_prototype::AbstractArray; friction_method::Union{FrictionEvaluationMethod, Nothing}) where {M<:Model}
    n = nstates(model)
    mat = NQCModels.QuantumModels.matrix_template(model, eltype(matrix_prototype))
    vec = NQCModels.QuantumModels.vector_template(model, eltype(matrix_prototype))

    potential = [Hermitian(zero(mat)) for _=1:beads]
    derivative = [Hermitian(zero(mat)) for _=1:ndofs(model), _=1:atoms, _=1:beads]
    adiabatic_derivative = [zero(mat) for _=1:ndofs(model), _=1:atoms, _=1:beads]
    eigen = [HermitianEigenWs(zero(mat)+I, vecs=true) for _=1:beads]
    phase_ref = [rand(eltype(matrix_prototype), n) .+ eps(eltype(matrix_prototype)) for _=1:beads]
    nonadiabatic_coupling = [zero(mat) for _=1:ndofs(model), _=1:atoms, _=1:beads]
    friction = [zero(mat) for _=1:ndofs(model), _=1:atoms, _=1:beads]
    tmp_mat = zero(mat)

    traceless_potential = [Hermitian(zero(mat)) for _=1:beads]
    V̄ = zeros(beads)
    traceless_derivative = [Hermitian(zero(mat)) for _=1:ndofs(model), _=1:atoms, _=1:beads]
    D̄ = zeros(ndofs(model), atoms, beads)
    traceless_adiabatic_derivative = [zero(mat) for _=1:ndofs(model), _=1:atoms, _=1:beads]

    centroid = zeros(eltype(matrix_prototype), ndofs(model), atoms)
    centroid_potential = Hermitian(zero(mat))
    centroid_derivative = [Hermitian(zero(mat)) for _=1:ndofs(model), _=1:atoms]
    centroid_eigen = HermitianEigenWs(zero(mat)+I, vecs=true)
    centroid_phase_ref = rand(eltype(matrix_prototype), n) .+ eps(eltype(matrix_prototype))
    centroid_adiabatic_derivative = [zero(mat) for _ in CartesianIndices(centroid_derivative)]
    centroid_nonadiabatic_coupling = [zero(mat) for _ in CartesianIndices(centroid_derivative)]
    centroid_friction = zeros(ndofs(model)*atoms, ndofs(model)*atoms)

    if friction_method isa Nothing
        @warn "No friction evaluation algorithm has been specified, the friction tensor will be empty"
    end

    return RingPolymer_QuantumFrictionModel_Cache(
        model,
        friction_method,
        potential,
        derivative,
        eigen,
        phase_ref,
        adiabatic_derivative,
        nonadiabatic_coupling,
        friction,
        tmp_mat,

        traceless_potential,
        V̄,
        traceless_derivative,
        D̄,
        traceless_adiabatic_derivative,

        centroid,
        centroid_potential,
        centroid_derivative,
        centroid_eigen,
        centroid_phase_ref,
        centroid_adiabatic_derivative,
        centroid_nonadiabatic_coupling,
        centroid_friction
    )
end


#Multiple dispatch definitions of the Create_Cache function with creates a Cache of the appropriate type dependent on what model type it is given
function Create_Cache(model::ClassicalModel, atoms::Integer, matrix_prototype::AbstractArray{T,N}) where {T, N}  #functions of this type were called Calculator(model, atoms, t)
    ClassicalModel_Cache(model, atoms, matrix_prototype)
end

function Create_Cache(model::ClassicalModel, atoms::Integer, beads::Integer, matrix_prototype::AbstractArray{T,N}) where {T, N}
    RingPolymer_ClassicalModel_Cache(model, atoms, beads, matrix_prototype)
end

function Create_Cache(model::ClassicalFrictionModel, atoms::Integer, matrix_prototype::AbstractArray{T,N}) where {T,N}
    ClassicalFrictionModel_Cache(model, atoms, matrix_prototype)
end

function Create_Cache(model::ClassicalFrictionModel, atoms::Integer, beads::Integer, matrix_prototype::AbstractArray{T,N}) where {T,N}
    RingPolymer_ClassicalFrictionModel_Cache(model, atoms, beads, matrix_prototype)
end

function Create_Cache(model::CompositeModel, atoms::Integer, matrix_prototype::AbstractArray{T,N}) where {T,N}
    if any([!isa(s.model, ClassicalModel) for s in NQCModels.get_pes_models(model.subsystems)])
        throw(ArgumentError("Currently, only CompositeModels using ClassicalModels to supply a PES are supported. "))
    end
    ClassicalFrictionModel_Cache(model, atoms, matrix_prototype)
end

function Create_Cache(model::CompositeModel, atoms::Integer, beads::Integer, matrix_prototype::AbstractArray{T,N}) where {T,N}
    if any([!isa(s.model, ClassicalModel) for s in NQCModels.get_pes_models(model.subsystems)])
        throw(ArgumentError("Currently, only CompositeModels using ClassicalModels to supply a PES are supported. "))
    end
    RingPolymer_ClassicalFrictionModel_Cache(model, atoms, beads, matrix_prototype)
end

function Create_Cache(model::QuantumModel, atoms::Integer, matrix_prototype::AbstractArray{T,N}) where {T,N}
    QuantumModel_Cache(model, atoms, matrix_prototype)
end

function Create_Cache(model::QuantumModel, atoms::Integer, beads::Integer, matrix_prototype::AbstractArray{T,N}) where {T,N}
    RingPolymer_QuantumModel_Cache(model, atoms, beads, matrix_prototype)
end

# specific multiple dispatch for AndersonHolstein to work with DiabaticMDEF, friction_methods needs to not be a keyword argument
function Create_Cache(model::QuantumModel, atoms::Integer, matrix_prototype::AbstractArray{T,N}, friction_method::Union{FrictionEvaluationMethod, Nothing}) where {T,N}
    QuantumFrictionModel_Cache(model, atoms, matrix_prototype; friction_method=friction_method)
end

function Create_Cache(model::QuantumFrictionModel, atoms::Integer, matrix_prototype::AbstractArray{T,N}; friction_method::Union{FrictionEvaluationMethod, Nothing}=nothing) where {T,N}
    QuantumFrictionModel_Cache(model, atoms, matrix_prototype; friction_method=friction_method)
end

function Create_Cache(model::QuantumFrictionModel, atoms::Integer, beads::Integer, matrix_prototype::AbstractArray{T,N}; friction_method::Union{FrictionEvaluationMethod, Nothing}=nothing) where {T,N}
    RingPolymer_QuantumFrictionModel_Cache(model, atoms, beads, matrix_prototype; friction_method=friction_method)
end
